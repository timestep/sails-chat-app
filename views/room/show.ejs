<h3><%= room.name %></h3>

<form id="outgoing-chat-form">
	<input size="50" type='text' id="outgoing-chat-field">
	<input type="submit">
</form>

<div id="incoming-chat-window"></div>

<!-- <table class="table">
	<tr>
		<td>ID</td>
		<td>Name</td>
		<th></th>
	</tr>

	<%_.each(rooms, function(room){ %>
		<tr>
			<td><%=room.id%></td>
			<td><%=room.name%></td>
			<td><a href="/room/show/<%=room.id%>" class="btn btn-sm btn-primary">JOIN</a></td>
		</tr>
	<% }) %>

</table> -->